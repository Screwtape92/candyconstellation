# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Azurite storage emulator data (Phase 5/6 local-first dev, per
# docs/architecture.md) - machine-local state, never checked in.
.azurite-data/

# Self-hosted (home box + ngrok) deployment's SQLite database - machine-local
# state, never checked in (docs/architecture.md "Self-hosted deployment").
/data/

# Playwright MCP's own debugging output (screenshots, console logs) from
# browser-driven test sessions - tool scratch, not project source.
/.playwright-mcp/
